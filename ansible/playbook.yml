---
- name: denvr
  hosts: all
  become: true
  tasks:
    
    - name: update & upgrade
      apt:
        upgrade: yes
        update_cache: yes

    - name: docker is present
      apt:
        name: docker
        state: present
        update_cache: yes
